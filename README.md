# 🏠 Boston House Price Prediction

A simple Flask web app to predict house prices using Linear Regression on the Boston dataset.

## 📁 Files Included
- `train_model.ipynb`: Trains the model and saves it as `model.pkl`
- `app.py`: Flask backend to handle input and show prediction
- `model.pkl`: Trained machine learning model
- `templates/index.html`: Frontend with custom CSS

## 🚀 How to Run
1. Clone/download this repository
2. Install dependencies: `pip install flask scikit-learn`
3. Run the app: `python app.py`
4. Open browser at `http://localhost:5000/`

## 🖼️ Screenshot
Add your screenshot here after running it locally!

## 🌐 Deploy (Optional)
You can deploy this on [Render](https://render.com/) or [Replit](https://replit.com/).
